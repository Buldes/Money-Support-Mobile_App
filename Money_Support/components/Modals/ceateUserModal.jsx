import React from "react";
import { StyleSheet, View, Modal } from "react-native";

const name = (props) => {
    const {} = props;

    const styles = StyleSheet.create({

    })

    return(
        <Modal>
            
        </Modal>
    );
}

export default name;