import React from "react";
import { StyleSheet, View } from "react-native";

const name = (props) => {
    const {} = props;

    const styles = StyleSheet.create({

    })

    return(
        <View>
            
        </View>
    );
}

export default name;